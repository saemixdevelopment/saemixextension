# 07/2026 - preparing workshop
** Bugfixes
- checkInitialFixedEffects
  - not updated yet (bug: Erreur dans if (model@modeltype == "structural")
  - ToDo:
    - fix condition
    - separate both plots as facet plots OR plot them separately for each response (one page per response)
- compare.saemix
  - not updated yet (bug: Erreur dans if (model@modeltype == "structural")
- data: issue with the discrete data datasets
  - rapi.saemix not included in the package
  - toenail.saemix not included in the package

** Improvements
- SaemixData
  - transformCatConv, transformContCov: create versions applicable to datasets which would use the colnames
    - change beginning of handson_pkpd.qmd to apply these functions before the creation of the object
  - plot with multiple responses
    - add the number of the response (eg Obs 1, Obs 2)
    - add a default facet plot option
- npde final diagnostic plots
  - stratify plot with Dose (currently only covariates)
    

# 03/2026
- bugfix in automated model selection procedures (Maud): corrected but need to push
- ToDo: remplacer par une fonction update qui pourrait s'appliquer à différents types de modèles (entrée=un objet + des arguments, sortie=la copie de l'objet avec les arguments modifiés)

# 05/2025
- fonctions de transformation de variables
  * fait: securiser les fonctions
  * fait: noms si on renomme, discrepancy in ocov...
  * ToDo: cas des NA pour les covariables catégorielles
  * ToDo: fonction pour discrétiser des covariables continues

* Check functions derviphi and dtransphi
    - completely unclear what these functions are supposed to do and whether they actually do it
    - especially for probit and logit models !
    - for h=exp() (phi=exp(phi), log-normal model for parameters)
        derivphi returns 1/exp(phi) (? 1/h' ?)
        dtransphi returns exp(phi) (could be h'())
        
* Error model
    - currently implemented as combined 1, consider switching to combined 2 => done
    Johannes 18/02/2021 suggests:
    error.typ<-function(f,ab) {
    -  g<-cutoff(ab[1]+ab[2]*abs(f)) 
    +  g<-cutoff(sqrt(ab[1]^2+ab[2]^2*f^2)) 
    return(g)
    
* createSaemixObject.initial
Currently, calling the createSaemixObject.initial function with a model parameter that isn't 'structural' will fail with the following error :

Error in (function (cl, name, valueClass)  : 
  assignment of an object of class “NULL” is not valid for @‘respar’ in an object of class “SaemixRes”; is(value, "numeric") is not TRUE

CHANGE fix.seed to be FALSE by default otherwise this can really mess up a simulation study...

* censored responses (12/05/21, Chris) bug
2.       If the column in the data supplied for name.cens is not named “cens”, saemix() throws an error.

# 05/2024
- calcul LL et SE sur la LL
  - comment est calculée la SE sur LL par IS dans Monolix ? (doc + tutos YouTube) ?
  - vérifier l'algorithme de calcul de LL par IS dans saemix
  - tester calcul avant et après conddist (temps calcul, qualité de la LL)
